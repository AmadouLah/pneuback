spring.config.import=optional:file:application-local.properties
spring.application.name=pneumaliback

server.port=${PORT:9999}
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-dawn-base-adk3hwl4.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&prepareThreshold=0}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false


# JWT Configuration
jwt.secret=${JWT_SECRET:9K8fL2mP5nR7tV9xC3dF6gH8jK1lM4nP7qS0tU3vW6yZ9A2bD5eG8hJ1kN4mP7rT0uX3vY6zA9B2cE5fH8iL1mN4pR7sU0vX3yZ6}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# Security Configuration
security.max-login-attempts=5
security.account-lockout-duration=30
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:*}

# Enable scheduling for token cleanup
spring.task.scheduling.pool.size=5

# Upload configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=60MB
storage.root=uploads

# Mail local (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:noreply@pneumali.ml}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.debug=false
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000

# Admin emails for weekly suspicious report (comma-separated)
app.admin.emails=admin@pneumali.ml,dev@pneumali.ml
